import { FieldLineDirective } from './field-line.directive';

describe('FieldLineDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldLineDirective();
    expect(directive).toBeTruthy();
  });
});
